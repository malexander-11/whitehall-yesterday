spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/whitehall_yesterday
    username: whitehall
    password: whitehall
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: ${SERVER_PORT:8080}

logging:
  level:
    root: INFO
    whitehall.yesterday: DEBUG
