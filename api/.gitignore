.gradle/
build/

# The wrapper JAR is a binary generated by `gradle wrapper --gradle-version 8.11.1`.
# Conventionally it is committed; we exclude it here because it cannot be generated
# as a text file. Generate it once and remove this line before committing.
gradle/wrapper/gradle-wrapper.jar
